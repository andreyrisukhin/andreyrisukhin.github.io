#!/usr/bin/env node
function test(e,s){s(),cases++}const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),scope={window:{}};vm.runInNewContext(fs.readFileSync(path.resolve(__dirname,"../assets/js/music/bayan-keyboard.js"),"utf8"),scope);const B=scope.window.BayanKeyboard;let cases=0;test("a pitch never changes columns when the visible range changes",(()=>{for(let e=48;e<61;e++){const s=B.layout(e,84).find((e=>72===e.midi));assert.equal(s.column,0),assert.equal(s.row,24)}})),test("column steps are three semitones and octaves retain their column",(()=>{for(let e=0;e<115;e++)assert.equal(B.position(e).column,B.position(e+12).column),assert.equal(B.position(e+3).row-B.position(e).row,1)})),test("visual columns run from high pitches to low pitches",(()=>{const e=B.html({low:60,high:72}).split("bayan-keyboard-column")[1];assert.ok(e.indexOf('data-midi="72"')<e.indexOf('data-midi="60"'))})),test("only exact MIDI voices are selected, not every octave",(()=>{const e=B.html({low:60,high:84,selected:[60,64,67],root:60});assert.equal((e.match(/is-selected/g)||[]).length,3),assert.equal((e.match(/is-root/g)||[]).length,1)})),test("out-of-range alignment cells are disabled",(()=>{const e=B.html({low:61,high:70});assert.ok(e.match(/data-midi="60"[^>]* disabled/)),assert.ok(e.match(/data-midi="71"[^>]* disabled/))})),test("labels and shortcuts cannot introduce markup",(()=>{const e=B.html({low:60,high:60,labels:{60:'"<img>'},shortcuts:{60:"<x>"},toggle:!0});assert.ok(!e.includes("<img>")),assert.ok(e.includes("&lt;x&gt;")),assert.ok(e.includes('aria-pressed="false"'))})),test("invalid ranges fail rather than producing an unbounded keyboard",(()=>{[[-1,60],[60,128],[72,60],[60.5,72]].forEach((([e,s])=>assert.throws((()=>B.layout(e,s)))))}));