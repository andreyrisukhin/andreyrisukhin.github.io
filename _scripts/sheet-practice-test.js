function check(e,t){t(),checks++}function harness(){let e=0,t=0,s=0;const a=new Map;let r=0;const n=[];let i,o=()=>Promise.resolve(c);const c={play:(e,t,s)=>n.push({midi:e,when:t,...s}),stop(){}};class u{constructor(){t++,this.state="running"}get currentTime(){return e}resume(){return Promise.resolve()}close(){return this.state="closed",Promise.resolve()}createGain(){return{gain:{value:1},connect(){}}}createDynamicsCompressor(){return{threshold:{},knee:{},ratio:{},attack:{},release:{},connect(){}}}}class l{constructor(){i=this,this.currentTime=0,this.paused=!0,this.readyState=1}addEventListener(){}play(){return this.paused=!1,Promise.resolve()}pause(){this.paused=!0}}const d={SheetPracticeTimeline:T,AudioContext:u,Soundfont:{instrument:()=>(s++,o())}};return vm.runInNewContext(fs.readFileSync(path.join(root,"assets/js/tactus/audio.js"),"utf8"),{self:d}),vm.runInNewContext(fs.readFileSync(path.join(root,"assets/js/sheet-music/practice-player.js"),"utf8"),{window:d,Audio:l,URL:{createObjectURL:()=>"blob:recording-test",revokeObjectURL(){}},fetch:async()=>({ok:!0,status:200,blob:async()=>({}),json:async()=>({measures:[{startSec:0,endSec:4},{startSec:4,endSec:7},{startSec:7,endSec:10}]})}),setInterval:e=>(a.set(++r,e),r),clearInterval:e=>a.delete(e),requestAnimationFrame:()=>1,cancelAnimationFrame(){}}),{create:d.SheetPracticePlayer.create,played:n,instrument:c,advance:t=>{e+=t,[...a.values()].forEach((e=>e()))},setLoad:e=>{o=e},get contexts(){return t},get loads(){return s},get audio(){return i},get timers(){return a.size}}}const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),root=path.resolve(__dirname,"..");let checks=0;const window={};vm.runInNewContext(fs.readFileSync(path.join(root,"assets/js/sheet-music/practice-timeline.js"),"utf8"),{window:window});const T=window.SheetPracticeTimeline,timeline=T.fromMeasures([{beats:4,notes:[{midi:60,beat:0,duration:4,tieStart:!0}]},{beats:4,tempos:[{beat:0,bpm:160}],notes:[{midi:60,beat:0,duration:4,tieStop:!0}]},{beats:4,tempos:[{beat:2,bpm:100}],notes:[{midi:64,beat:0,duration:4}]}]);check("Tempo changes, including mid-measure changes, set measure boundaries",(()=>{assert.deepEqual(Array.from(timeline.starts),[0,2,3.5]),assert.equal(timeline.duration,5.45)})),check("A tied note crosses the tempo boundary without a second attack",(()=>{assert.equal(timeline.events.length,2),assert.equal(timeline.events[0].start,0),assert.equal(timeline.events[0].end,3.5)})),check("Duration uses all tempo segments crossed by a note",(()=>assert.equal(timeline.events[1].end,5.45))),check("Same-pitch notes in different staves are not merged",(()=>{const e=T.fromMeasures([{beats:4,notes:[{midi:60,beat:0,duration:1,tieStart:!0,staff:1},{midi:60,beat:1,duration:1,tieStop:!0,staff:2}]}]);assert.equal(e.events.length,2)})),check("Explicit opening tempo overrides the default",(()=>{assert.equal(T.fromMeasures([{beats:4,tempos:[{beat:0,bpm:60}]}]).duration,4)})),check("Invalid or mismatched recording timing is rejected",(()=>{assert.throws((()=>T.renderedMap({measures:[]},3))),assert.throws((()=>T.renderedMap({measures:[{startSec:1,endSec:0}]},1))),assert.throws((()=>T.renderedMap({measures:[{startSec:0,endSec:1},{startSec:2,endSec:3}]},2)))})),check("Production recording ends at the score, not the reverb tail",(()=>{const e=JSON.parse(fs.readFileSync(path.join(root,"assets/music/sheet-music/cogwork-dancers/cogwork-dancers-timing.json")));assert.equal(T.renderedMap(e,79).duration,137.9)})),(async()=>{const e=harness(),t=e.create(timeline);check("No autoplay or audio context during initialization",(()=>{assert.equal(t.snapshot().state,"paused"),assert.equal(e.contexts,0)})),check("Invalid speed and reversed/out-of-range loops leave state intact",(()=>{assert.equal(t.setRate(0),!1),assert.equal(t.setRate(NaN),!1),assert.equal(t.setLoop(3,2,!0),!1),assert.equal(t.setLoop(0,2,!0),!1),assert.equal(t.setLoop(1,4,!0),!1),assert.equal(t.seek(NaN),!1)})),t.setRate(.5),await t.play(),check("Half speed doubles live note duration without transposing",(()=>{assert.equal(e.played[0].midi,60),assert.equal(e.played[0].duration,7),assert.equal(t.snapshot().state,"playing")})),t.pause(),check("Pause cancels the live scheduler",(()=>assert.equal(e.timers,0))),t.seek(2),await t.play(),check("Seeking into a tied note resumes its remaining duration",(()=>{assert.equal(e.played.at(-1).midi,60),assert.equal(e.played.at(-1).duration,3)})),t.pause(),t.setRate(1),t.setLoop(2,2,!0),t.restart(),await t.play(),e.advance(2),check("Single-measure loop restarts at its first measure",(()=>{assert.equal(t.snapshot().measure,2),assert.equal(t.snapshot().state,"playing"),assert.ok(e.played.at(-1).duration<=1.5)})),t.pause(),t.seek(3),check("Seeking outside a loop explicitly disables the loop",(()=>assert.equal(t.snapshot().loop.enabled,!1))),await t.play(),e.advance(3),check("Natural end stops the scheduler and resets position",(()=>{assert.equal(t.snapshot().state,"paused"),assert.equal(t.snapshot().measure,1),assert.equal(e.timers,0)}));const s=harness();let a;s.setLoad((()=>new Promise((e=>{a=e}))));const r=s.create(timeline),n=r.play();r.pause(),a(s.instrument),await n,check("Pause while sound loads prevents late playback",(()=>{assert.equal(r.snapshot().state,"paused"),assert.equal(s.played.length,0)}));const i=harness(),o=i.create(timeline,{audioUrl:"test.mp3",timingUrl:"test.json"});o.seek(2),o.setRate(.75),await o.play(),check("Recording uses its own timing map and pitch-preserving speed",(()=>{assert.equal(i.audio.currentTime,4),assert.equal(i.audio.playbackRate,.75),assert.equal(i.audio.preservesPitch,!0)})),o.setMode("live"),check("Switching sound preserves the measure without autoplay",(()=>{assert.equal(o.snapshot().measure,2),assert.equal(o.snapshot().state,"paused"),assert.equal(i.audio.paused,!0)})),o.setMode("recording"),o.setLoop(2,2,!0),await o.play(),i.audio.currentTime=7.1,i.advance(0),check("Recording loops at the selected end, excluding following measures",(()=>assert.equal(i.audio.currentTime,4))),o.dispose(),check("Disposal stops audio and timers",(()=>{assert.equal(i.audio.paused,!0),assert.equal(i.timers,0)}))})().catch((()=>{process.exitCode=1}));